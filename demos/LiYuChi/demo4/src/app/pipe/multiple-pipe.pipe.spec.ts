import { MultiplePipePipe } from './multiple-pipe.pipe';

describe('MultiplePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplePipePipe();
    expect(pipe).toBeTruthy();
  });
});
